const Game = require("../engine/Game");
const Match = require("../models/match");

class MatchManager {
    constructor() {
        this.matches = new Map();
    }

    createMatch(hostId) {
        
    }
    
}